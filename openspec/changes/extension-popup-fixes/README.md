# extension-popup-fixes

Extension popups: fix async/React popups collapsing to a blank 1x1 viewport (force default size), make popups transient/closable, mark contexts inspectable. Proton Pass stays on the shim (its MV3 background SW doesn't start in the generic runtime)
