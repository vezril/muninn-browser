# Tasks: autocomplete

## 1. History ranking

- [x] 1.1 `HistoryEntry.visits`; `record` increments + moves to front; `rankedHosts()` (visits then recency); `bestCompletion(for:)`.

## 2. Address bar

- [x] 2.1 `NSTextFieldDelegate` inline completion (complete-at-end, suffix selected; Tab / → accepts; no complete while deleting).

## 3. New-tab search

- [x] 3.1 Inject ranked hosts into the landing page; inline autocomplete JS on the input (Tab/→ accept).

## 4. Ship

- [x] 4.1 Full suite green; ship PR-gated; update `CLAUDE.md`; cut version.
