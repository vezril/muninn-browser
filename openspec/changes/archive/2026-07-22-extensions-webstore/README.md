# extensions-webstore

Install extensions from the Chrome Web Store (URL/ID → CRX download → strip → unpack); extension action icons move to the address row so they never clip on a narrow sidebar; hermetic ExtensionManager under test
