# Tasks: debounce-and-fingerprint2

- [x] `Debouncer` (curated host[+path]→param rules; recover destination) + 4 unit tests
- [x] `ShieldsManager.debounce` flag (default on) + `sessionToken` (per-launch)
- [x] `decideNavigation` debounces before query-strip; gated + per-site
- [x] Fingerprinting 2.0: per-session/eTLD+1 seed, measureText, hardwareConcurrency, navigator.languages
- [x] Settings → Shields toggle + panel row
- [x] Live-verified (Calvin)
- [ ] Ship: full suite green; version bump + tag; archive
