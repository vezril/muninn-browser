# pomodoro-timer

Customizable Pomodoro timer as a Tools-sidebar tool: progress ring + countdown, start/pause/reset/skip, session dots, inline duration customization, phase-end sound + in-app toast + OS notification
