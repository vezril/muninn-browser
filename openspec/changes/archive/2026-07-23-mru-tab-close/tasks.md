# Tasks: mru-tab-close

- [x] `closeTab` selects the most-recently-used remaining workspace tab (max `lastActiveAt`) and re-stamps it.
- [x] Build clean; suite green (110); live-gated (⌘W returns to the previous tab, not tab #1).
