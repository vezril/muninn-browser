# status-bar-weather

Status bar above the web content showing weather (temperature, humidity, US AQI) for a configurable city (default Montreal) via Open-Meteo; new Status Bar settings section, toggleable, extensible
