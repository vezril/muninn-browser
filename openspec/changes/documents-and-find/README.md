# documents-and-find

Local files + document viewing + find-in-page: open local files (address bar path/file:// + drag-drop, loadFileURL), built-in JSON viewer (prettify/colour/collapsible tree), PDF download (Cmd+S/right-click + wire native HUD button via WKUIDelegate SPI), and Cmd+F find bar
