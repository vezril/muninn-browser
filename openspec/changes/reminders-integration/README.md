# reminders-integration

Apple Reminders integration (EventKit): sidebar tool to view/create/complete reminders and lists; commands incl. page-context (page→reminder, page→list from recipe structured data or local model)
