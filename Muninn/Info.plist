<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Apple Reminders integration (EventKit): the Reminders tool reads and writes the
	     user's reminders and lists on-device. -->
	<key>NSRemindersFullAccessUsageDescription</key>
	<string>Muninn shows your reminders in the sidebar and can create reminders and lists — for example, turning a recipe page into a shopping list.</string>
	<!-- Register Muninn as an http/https handler so it can be chosen as the default
	     web browser; incoming links are routed to AppDelegate.application(_:open:). -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>Web site URL</string>
			<key>CFBundleTypeRole</key>
			<string>Viewer</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>http</string>
				<string>https</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
