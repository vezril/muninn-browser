# FR-25 re-grep artifact — 2026-07-11

- WebClients commit: `ea9021f3cd9d4e82f83a8da23b4e36a0a7d2f538`
- Pass extension version (manifest-chrome.json): `1.38.2`
- Vendored bundle version: `1.38.0`
- Baseline: Spike B (2026-07-11, v1.38.2) via `tools/regrep-baseline.txt`

## Safari-manifest permission profile

```json
{
  "permissions": [
    "activeTab",
    "alarms",
    "nativeMessaging",
    "scripting",
    "storage",
    "unlimitedStorage",
    "clipboardWrite",
    "webNavigation"
  ],
  "host_permissions": [
    "*://*/*"
  ],
  "optional_permissions": null
}
```

## Call-site inventory (browser.*/chrome.* members, by count)

```
  25 alarms.create
  22 runtime.getURL
  22 privacy.services
  13 webNavigation.getFrame
  12 alarms.get
  11 alarms.clear
  10 tabs.sendMessage
  10 tabs.get
  10 storage.local
   9 alarms.onAlarm
   8 storage.session
   8 runtime.onMessage
   7 tabs.query
   7 tabs.create
   7 runtime.sendMessage
   7 action.setBadgeText
   6 runtime.lastError
   6 permissions.request
   6 permissions.contains
   5 webNavigation.getAllFrames
   5 runtime.reload
   5 runtime.id
   4 webRequest.onCompleted
   4 tabs.remove
   4 tabs.getCurrent
   3 webRequest.onErrorOccurred
   3 webRequest.onAuthRequired
   3 tabs.update
   3 tabs.onUpdated
   3 scripting.executeScript
   3 runtime.connectNative
   3 permissions.onAdded
   2 windows.update
   2 windows.create
   2 runtime.requestUpdateCheck
   2 runtime.getFrameId
   2 runtime.connect
   2 permissions.onRemoved
   2 action.setBadgeBackgroundColor
   1 webRequest.onBeforeSendHeaders
   1 webRequest.onBeforeRequest
   1 tabs.onRemoved
   1 scripting.insertCSS
   1 runtime.sendNativeMessage
   1 runtime.onUpdateAvailable
   1 runtime.onStartup
   1 runtime.onMessageExternal
   1 runtime.onInstalled
   1 runtime.onConnect
   1 runtime.getManifest
   1 runtime.getBrowserInfo
   1 runtime.getBackgroundPage
   1 commands.onCommand
   1 commands.getAll
   1 alarms.clearAll
```

## Diff vs baseline

### NEW (must be triaged Tier 1/2/3 below before shim work proceeds)

- [x] `commands.getAll` — TRIAGE: **Tier 3 — skip.** `commands` is absent from the Safari manifest (profile above); Spike B already recorded the namespace as skip. Baseline omission, not new surface. Inert stub only if boot probing demands it (S1).
- [x] `commands.onCommand` — TRIAGE: **Tier 3 — skip.** Same as above.
- [x] `privacy.services` — TRIAGE: **Tier 3 — skip.** Absent from Safari manifest; Spike B recorded exactly these 22 refs (count unchanged). Baseline omission, not new surface.
- [x] `runtime.getBackgroundPage` — TRIAGE: **Tier 1 — inert stub.** One call site; MV2-era API removed in Chrome MV3, so it is almost certainly guarded off the Safari/MV3 path. Stub returns rejection per MV3 semantics; S1's boot log (E3) verifies it is never hit un-guarded.
- [x] `runtime.getFrameId` — TRIAGE: **Tier 2 — FrameRegistry.** Genuinely new since Spike B (2 call sites): computes the frame ID of a window target. Folds into FR-9's FrameRegistry surface (E5) — mechanical addition, same bookkeeping the registry already does for `webNavigation.getFrame`.
- [x] `webRequest.onBeforeSendHeaders` — TRIAGE: **Tier 3 — skip.** `webRequest` remains absent from the Safari manifest; Spike B recorded the namespace as skip (this member just wasn't enumerated). Behind BUILD_TARGET guards.

### RETIRED (in baseline, no longer found — note, no action required)

- `storage.onChanged`
- `webNavigation.onCommitted`
- `webNavigation.onDOMContentLoaded`

## Triage

**Summary (2026-07-11): gate SATISFIED — zero untriaged entries.** Five of six NEW entries are baseline omissions or Tier-3 skips already covered by Spike B's dispositions; the one genuinely new API since Spike B is `runtime.getFrameId` → **Tier 2, assigned to E5's FrameRegistry**. No Tier-2 subsystem is added; the architecture is unaffected.

Notes:
- **Version skew (task 4.4):** vendored bundle is v1.38.0 (Safari/MAS channel), WebClients main is at v1.38.2 — the structural MAS lag ADR-001/architecture §8 risk 5 predicted. Skew is at patch level; the Safari manifest permission profile is identical, so the vendored bundle is valid against this inventory.
- The three RETIRED entries were speculative over-inclusions in the initial baseline (Spike B's table did not enumerate them); additionally this grep only matches literal `browser.<ns>.<member>` accesses, so event wiring routed through wrappers may not surface — same method and caveat as Spike B itself.
- Safari manifest continues to require `nativeMessaging` — consistent with ADR-005's boot-precondition stub (FR-12 promotion, ratified at the architecture gate).
