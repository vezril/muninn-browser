# CLAUDE.md — Muninn Browser

Muninn is a privacy-first, Apple-native macOS browser with a fully custom shell, built on **WKWebView** with a purpose-built **Proton Pass API shim** (no general extension platform). Swift/AppKit for shell + shim runtime; Scala reserved for the future sync/service layer.

## Where this project stands (artifact state)

Analysis, Planning, and Solutioning (architecture) are **complete and approved** (PRD + roadmap: 2026-07-11 21:14 EDT; architecture + ADRs: 2026-07-11 22:55 EDT). The artifacts in this directory are the record:

- `product-brief.md` — vision, MVP (walking skeleton), constraints, open questions. **Start here.**
- `decisions.md` — locked decisions (name, engine, language split, fallbacks). Do not re-litigate without new evidence.
- `research/spike-a-results.md` — engine spike (CEF/JCEF ruled out; signed off 2026-07-11).
- `research/spike-b-proton-pass-api-inventory.md` — the shim spec (Proton's Safari build profile; ~45 methods; Tier 1/2/3 breakdown; ordered risk list).
- `prd.md` — v0.1 **APPROVED** (2026-07-11): FR-1…29, NFR-1…10, MVP cutline (§9), resolutions to all open questions (§12). The locked "what".
- `roadmap.md` — v0.1 **APPROVED**: milestones M0–M3 + Sync horizon, epics E1–E12, full FR→epic traceability, dependency DAG. The plan of record.
- `architecture.md` — v0.1 **APPROVED**: HLD (closed-microkernel modular monolith, C4 diagrams, 9-risk table, 3 ratified deviations in §10).
- `adr/ADR-001…008` — all **Accepted**; ADR-002 and ADR-006 carry open spikes (S5: pktap eproc + proxy routing, gates E8; S6: scheme-request initiator identification, gates E4). Spikes S1/S2 gate E3/E6 (see ADR-001/-005).
- `openspec/changes/architecture-and-adrs/` — the completed Solutioning change (research evidence under `research/`).

**M0 is EXITED** (Calvin, 2026-07-11 23:35 EDT — see `roadmap.md` §6): the Xcode scaffold builds/launches, the FR-25 gate is satisfied (`research/regrep/2026-07-11.md`; new API `runtime.getFrameId` → Tier 2/E5), and the Pass bundle v1.38.0 is vendored (`vendor/pass-extension/`, `tools/refresh-pass-bundle.sh`).

**E2+E3 are built (change `e2-e3-shim-core`): the shim core + background host.** S1 is retired — Proton's real v1.38.0 `background.js` boots CLEAN in the DedicatedWorker substrate (ADR-005 refined 2026-07-12: background.js runs in a Worker inside the hidden WKWebView because it uses `importScripts`). 16 XCTests green; refute-review blockers all fixed. Ports deferred to E6 (need a 2nd context). **E3-hardening + S2 are done (change `e3-hardening-s2`).** Timer throttling FIXED with a **public** lever (`WKPreferences.inactiveSchedulingPolicy = .none`) — bisect: 125/120 ticks vs the 4/120 default baseline; zero private API in the shipping path (`research/nfr10-residency-2026-07-12-post-mitigation.md`). S2 spike GO: the shim is provably confined to an isolated `WKContentWorld` — page MAIN world has no `chrome`/`browser`/broker handler; the account app's postMessage fallback is selected by corollary (`research/s2-fork-fallback-2026-07-12.md`). Root-caused via the webkit-developer agent (the throttle is RunningBoard suspension + App Nap, not DOM-timer throttling; an off-screen window was refuted). 22 XCTests green.

**Two carries into E6:** (1) the host now uses a dedicated `WKWebsiteDataStore` (for the per-process throttle latch) — verify the auth-fork handshake doesn't depend on a shared cookie store with `*.proton.me` tabs; (2) the native→content inbound push path is unwired (login uses the postMessage fallback, not `onMessageExternal`, so out of S2 scope — E5/E6 wire it).

**E6 (auth-fork login) is CHECKPOINTED at a precise, non-D4 blocker** (`research/e6-auth-fork-2026-07-17.md`). Built + green (23 tests): the minimal navigable shell (`AppShell`), the cross-context request/response **message bus** (page `runtime.sendMessage` → host worker `onMessage` → `sendResponse`, under canonical id), and the fork trigger (`runtime.onInstalled` → `background.js` `tabs.create` → the onboarding URL, now wired to the shell). Two live login attempts (Calvin at the keyboard, no credentials touched): the onboarding page loads with correct fork params, but the account app reports **"Proton Pass is missing permissions"** — because we inject only `fork.js`, not `orchestrator.js` (the general content script the account app's extension-detection depends on). **This couples E6 to E5.**

**SIDEBAR — Arc-style, shipped (change `arc-sidebar`, 2026-07-21).** The left sidebar now has: **favourites** (favicon tiles) + **pinned tabs** + **folders** (collapsible, renamable, colour-coded headers) + regular tabs, with full **drag-and-drop** (reorder within a section, move between sections, in/out of folders, reorder folders; insertion-line indicator) and per-site **favicons** (fetched from each site's own origin — never a third-party favicon service). **Workspaces** live in a bottom switcher: each owns its favourites/pins/folders/regular tabs, with an emoji icon (visual picker), a live background colour that tints the whole window, `⌃1…⌃9` shortcuts, and a crossfade on switch. Window chrome: transparent title bar with full-size content (tint extends behind the traffic lights, no title text), nav cluster beside the traffic lights, and the web view as a floating rounded card. Collapse is **Arc-style hover-peek**: the sidebar (with traffic lights + nav) slides off; hovering the left edge peeks it back as a floating, rounded, shadowed overlay; the toggle lives only inside the sidebar. Persisted to `sidebar.json` (`SidebarStore`/`SidebarState`).

**AUTO-ARCHIVE (change `auto-archive`, 2026-07-21).** Regular, ungrouped tabs idle past a threshold auto-close (exempt: active/pinned/favourite/split/mini; reopenable via Cmd+Shift+T/history). `BrowserTab.lastActiveAt` + `archiveStaleTabs()` (sweep on tab switch + ~5-min timer); interval in Settings → **Auto-Archive Tabs ▸** (`AutoArchive.current`, default 1 day). The pin/regular **separator** has a **"↓ Clear"** button (right side) → `clearUnpinnedTabs`, shown only when there are unpinned tabs.

**SETTINGS WINDOW (change `settings-window`, 2026-07-21).** A full Settings window (`SettingsWindowController`, ⌘,) — top-toolbar nav (icons) for **General / Profiles / Shortcuts / Advanced**; iOS-style `NSSwitch` toggles; rows are label-left/control-right. **General:** Warn before quitting (`AppSettings` + `applicationShouldTerminate`). **Profiles** (master-detail, list top-aligned + space counts + ＋/−/✎): per-profile Search engine, search suggestions, Archive tabs, **Download location** — these moved from global to per-`Profile` fields; downloads via `WKDownloadDelegate` → the tab's profile folder. **Shortcuts:** remappable — `Shortcut`/`ShortcutAction`/`ShortcutStore` + `ShortcutRecorder`; the key monitor reads the store (⌃1–9 fixed); menus dropped duplicate key-equivalents.

**LIBRARY (change `library`, 2026-07-22).** A **Library** for **Downloads + Media** (Arc-style, scoped to those two per Calvin). **Download tracking:** `DownloadStore`/`DownloadRecord` → `downloads.json`, fed by `InjectionCoordinator.onDownloadFinished` (added `downloadDidFinish`/`didFail` + a `downloadInfo` map for dest/source). **Tracked right-click saves:** WebKit's native context-menu save **bypasses `WKDownloadDelegate`**, so right-click saves never recorded — fixed by injecting a MAIN-world `contextmenu` listener (`ContextMenuHandler` message handler) that captures the clicked image/link, `MuninnWebView` adding **"Save Image"** / **"Download Linked File"** items that route through `InjectionCoordinator.startDownload` (→ `WKWebView.startDownload`, tracked), and **removing WebKit's native "Download Image"**. **`LibraryPane`** — a **left-side overlay** (not a window) that slides in over everything, **workspace-tinted** (`currentTintColor()`) with **rounded corners** + shadow; **Downloads** list (icon, name, source·size·date; double-click open; hover reveal/remove) + **Media** grid (image/video/audio downloads, image previews). Dismiss on click-outside / × / button-toggle. **Sidebar 📚 button** bottom-left beside the workspace switcher (`HoverIconButton` — hover highlight + pointing-hand). On download finish, a **drop animation** (`flyToLibrary`: a file icon falls straight down into the button) + accent flash. Downloads tracked going forward only.

**LOCAL MODELS / OLLAMA (change `ollama-connection`, 2026-07-21).** Run prompts against a **local Ollama** daemon — local-first, no data leaves the machine, no shim/credentials. **`OllamaClient`** (native `URLSession`): `GET /api/tags`, streaming `POST /api/chat` (multi-turn) + `/api/generate`, NDJSON parsed incrementally (`OllamaSettings` persists base URL default `http://localhost:11434` + default model). **Settings → Models** (new nav section): base URL, **Test Connection** (lists installed models), default-model picker. **Ask chat tool** in the Tools sidebar — a **non-blocking** streamed chat (an earlier modal popup blocked the UI during generation, so it was replaced by a sidebar tool): `AskChatView` streams via an `AppShell.runChat` closure, keeps browsing usable, multi-turn, with a **typing indicator** (. → .. → …) until the first token. **Sessions persist** to `chat.json` (`ChatStore`/`ChatSession`/`ChatMessage`) with a switcher + New Session + Clear. **`ToolsSidebar` is now multi-tool** (`setTools`/`selectTool` + a Calendar/Ask switcher, replacing single-tool `setTool`). **⌘N → "Ask Local Model…"** reveals the Ask tool. **Gotchas fixed:** the chat scroll's documentView must be pinned to `scroll.contentView.widthAnchor` (not the frame — the scroller strip caused right-aligned bubbles to clip); and a per-row `widthAnchor == messagesStack.widthAnchor` must be activated **after** `addArrangedSubview` (activating across no-common-ancestor views throws and silently aborts the render). 8 parser tests. **Page context added (change `page-context`):** a **📄 toggle** left of the Ask input attaches the active tab's title/URL/visible text as a hidden `system` message (`ChatMessage.Role.system`) prepended to the turn — `AskChatView.fetchPageContext` closure → `AppShell` runs `evaluateJavaScript("document.body.innerText")` (~12k cap); reflects the current tab, not persisted, stays local. So "summarize this page" works.

**COMMAND PALETTE ACTIONS (change `palette-commands`, 2026-07-21).** The ⌘N palette now runs **app commands**, not just search/URL/tab/history. `CommandPalette.Command` + a `.command(id)` `Item.Kind`; `AppShell.paletteCommands()` supplies the list and `runPaletteCommand(id)` dispatches to existing methods. Commands are listed ahead of tabs/history but **after** the search/go line (so Enter on a normal query still searches). Each shows its remappable shortcut (from `ShortcutStore`). Initial set: Pin/Unpin/Favourite/Unfavourite Current Tab, Clean Up (=`clearUnpinnedTabs`), Toggle Sidebar, Toggle Tools Sidebar, Open Last Tab (=`reopenLastClosed`), Reload, Copy URL, Open Settings, and **Switch Space** — one entry per workspace (icon+name, `id="space:<uuid>"`) so typing a space name autocompletes. Dev-mode adds Open Inspector / View Page Source. **Growing it = one entry in `paletteCommands()` + one case in `runPaletteCommand()`** (the intended pattern as more tools land).

**DEVELOPER MODE (change `developer-mode`, 2026-07-21).** A **Developer Mode** toggle (Settings → Advanced, `AppSettings.developerMode`, off by default) adds right-click **View Page Source** + **Inspect Element** and the **⌥⌘U / ⌥⌘I** shortcuts. All web views are now `MuninnWebView : WKWebView` (created by `InjectionCoordinator`); `willOpenMenu` adds the dev items only in dev mode. **View Page Source** → serialized `outerHTML` in a new tab. **Inspect** opens the real WebKit Web Inspector **in-app** via **private API** (Calvin's choice over public-only) — the recipe (root-caused with the webkit-developer agent against WebKit source): (1) `isInspectable` alone is insufficient for the *local* frontend — must also set `WKPreferences` **`developerExtrasEnabled`** on the *config* at web-view creation; (2) `WKWebView._inspector` → `-show`; (3) WebKit opens it **docked**, reparented into the inspected view's superview — which is our clipping rounded web card → **blank + flicker**, so we **force detached**: poll `_inspector.isVisible` then `-detach` (which persists `InspectorStartsAttached=NO`), and **pre-seed** `__WebInspectorPageGroupLevel1__.WebKit2InspectorStartsAttached=false` at launch so even the first open is detached. Fails closed (missing symbol → no-op). **App Store caveat** (recorded in the change proposal): private symbols; gate behind a build flag before any MAS submission.

**LIVE CALENDARS + TOOLS SIDEBAR (change `live-calendars`, 2026-07-21).** A new **right-side Tools sidebar** (`ToolsSidebar`, collapsible, workspace-tinted, top-right `sidebar.right` toggle + **⌘⌥T**, persisted `SidebarState.toolsSidebarOpen`) hosts a stack of tool widgets — the first is the **Live Calendar** (Arc "Live Calendars" for Proton). Since Proton Calendar has **no API/CalDAV** (E2E-encrypted), the data source is **Option B: a user-pasted read-only ICS share link** fetched natively (`CalendarFeed`, `URLSession`, `webcal://`→`https`, 5-min poll + refresh-on-open, per-calendar last-good cache — no shim, no credentials). Parsing is a pure, fixture-tested core: `ICSParser` (line unfolding, params, **TZID via Foundation `TimeZone`** rather than VTIMEZONE offset rules, DURATION, join-link heuristics) + `Recurrence` (full RFC 5545: `FREQ`/`INTERVAL`/`COUNT`/`UNTIL`/`BYDAY`/`BYMONTHDAY`/`BYMONTH`/`BYSETPOS`/`WKST`, `EXDATE`/`RDATE`, `nextOccurrence`) — **19 XCTests**. `LiveCalendarWidget` shows the next event + a 1-s live countdown + a **Join** button (gated by per-calendar lead time + a detected Meet/Zoom/Proton-Meet/Teams link) that opens via `openRouted` (composes with Air Traffic Control). Managed in **Settings → Calendars** (name + share URL + lead time). Feasibility write-up: `research/live-calendars-feasibility-2026-07-21.md`. Live-gated against Calvin's real Proton calendar.

**AIR TRAFFIC CONTROL (change `air-traffic-control`, 2026-07-21).** User-defined **routing rules** (`RoutingRule`: `host → workspaceId`, suffix-aware `matches` so `github.com` also matches `gist.github.com`; persisted in `SidebarState.routingRules`). Every user-facing URL entry point funnels through one chokepoint, `AppShell.openRouted(_:newTab:)`: if a rule matches and points at a *different* space, switch to it (→ its profile) and open the URL in a new tab there; else load in place / new tab. Wired into the **address bar** (`navigate`), the **command tool** (`CommandPalette` `.url`), and **external links** (`AppDelegate.application(_:open:)` → `route`, which routes or falls back to a Quick Look). Managed in **Settings → Routing** (5th nav item): host field + space picker + trash-remove per row, "Add Rule", empty state. **Gotcha:** the command palette's `.url` case previously called `openInNewTab` directly, bypassing routing — centralising through `openRouted` was the fix. (Debug aside: a scratchpad-path file logger wrote nothing because the GUI app can't write under `/private/tmp/claude-501`; logging to `~/Library/Application Support/Muninn/` proved the path.)

**SETTINGS (change `settings`, 2026-07-21).** A **gear button** on the right of the URL bar → menu: **Search Engine** ▸ (DuckDuckGo/Google/Bing, `SearchEngine.current` persisted — drives address bar, command bar, and new-tab search), **Set as Default Browser…**, and **Clear This Profile's Data…** (history + `WKWebsiteDataStore` for the current profile, confirmed). The **address bar now searches** non-URL input with the configured engine (previously URL-only).

**PROFILES (change `profiles`, 2026-07-21).** Separate work/personal browsing: a `Profile` (persisted in `SidebarState.profiles`) owns an isolated session and each **Workspace belongs to a profile** (`Workspace.profileId`). Isolation = a persistent `WKWebsiteDataStore(forIdentifier:)` per profile (cookies/logins/storage; the **default** profile keeps the shared `.default()` store so existing logins survive) **plus per-profile `HistoryStore`** (`history-<id>.json`) — recording, autocomplete, new-tab suggestions, and the command bar all use the active workspace's profile's history. `InjectionCoordinator`/`BrowserTab` take a data store; `makeTab(workspaceId:)` picks the workspace's profile store. Assign via the workspace context menu → **Profile ▸** (or New Profile…); assigning re-creates the workspace's tabs in the new jar.

**AUTOCOMPLETE (change `autocomplete`, 2026-07-21).** History-backed inline autocomplete in the address bar (`NSTextFieldDelegate`, complete-at-end with the suffix selected; Tab/→ accepts) and the new-tab search (ranked hosts injected into the landing page + the same JS). `HistoryStore` counts **visits** (frequency) and ranks hosts by visits→recency (`rankedHosts()`/`bestCompletion(for:)`). Suggestions come only from visited sites.

**MINI PLAYER (change `mini-player`, 2026-07-21).** Switching away from a media-playing tab (select another, Cmd+T, or workspace switch) pops its video into a floating always-on-top **`MiniPlayerWindow`** (titled/chromeless → native drag + edge-resize); the tab's live web view is borrowed so audio+video keep playing. Video-only: the `<video>` is reparented into a root fullscreen wrapper with `!important` sizing (survives YouTube's transformed ancestors). Controls: play/pause, return-to-tab, close; click the video to toggle (icon-flash ⏸/▶ rendered **in the web view** — an AppKit overlay won't composite above WKWebView; `acceptsFirstMouse` so the first click counts). Detection: isolated-world probe on `play`/`playing`/`pause`/`ended` (**no readyState gate** — else the initial buffering play reads as not-playing).

**PREVIEWS (change `previews`, 2026-07-21).** Hover a **favourite** icon (~0.5s) → a live, interactive **preview popover** of the site (`NSPopover`, reused `InjectionCoordinator` web view on the shared host/data store; shared cookies → logged-in). Hovering the popover keeps it open; leaving closes it (blanked to free the page); clicking still opens the favourite fully.

**PEEK (change `peek`, 2026-07-21).** Clicking a **cross-site** link in a pinned/favourite tab opens a **Peek** preview (`PeekOverlay`, slides in from the right, dims the anchored tab) instead of navigating the tab away from its `homeURL`; **Open in Tab** promotes it, Esc/×/dim-click dismisses. Intercepts both `decidePolicyFor` link clicks and `target="_blank"`/`window.open` (`WKUIDelegate.createWebViewWith`; regular tabs get a normal new tab). **Gotcha:** the SDK's `decidePolicyFor` `decisionHandler` is `WK_SWIFT_UI_ACTOR` — the Swift closure MUST be `@escaping @MainActor` or Swift silently doesn't install the delegate method. Peek is created on the next runloop tick (making a web view inside the policy callback re-enters WebKit).

**SPLIT VIEW (change `split-view`, 2026-07-21).** 2–4 regular tabs side by side in an `NSSplitView` (resizable dividers); each pane a rounded card with an active-accent border + hover × (leaves the split). A split renders as **one combined sidebar tab** with per-member mini-chips (favicon + title + ×). Create by **dragging a tab onto the center** of another (3-zone drop: center=split, edges=reorder) or right-click **Add to Split View**; closing a member removes it from the split (dissolves at ≤1). `BrowserTab.splitGroupId` (session-only, regular tabs); address/back/forward act on the active pane.

**QUICK LOOK — "Little Muninn" (change `quick-look`, 2026-07-21).** A compact ephemeral window (`QuickLookWindow`) for a quick lookup you then triage: **Cmd+Option+N** / File → New Quick Look, or an external link when Muninn is the **default browser** (`Info.plist` `CFBundleURLTypes` http/https → `AppDelegate.application(_:open:)`; **Set as Default Browser…** menu item). It hosts a shim-injected WKWebView on the shared background host; **Esc/close** tosses it, **Open in Muninn** promotes the page to a main-window tab. Main-window shortcuts are gated to the main window.

**SHORTCUTS + COMMAND BAR (change `shortcuts-command-bar`, 2026-07-21).** A `CommandPalette` overlay on **Cmd+N** (current window): search (DuckDuckGo), open a URL, switch to an open tab, or jump to a persisted-`HistoryStore` entry. Shortcuts: **Cmd+W** (pinned/favourite tabs *unload* to free memory but stay in the sidebar; regular tabs removed + remembered for **Cmd+Shift+T** reopen), **Cmd+D** pin toggle, **Cmd+Shift+C** copy URL, **Cmd+Shift+Option+C** copy as Markdown, **Cmd+Shift+K** clear unpinned; standard Cmd A/C/V/X/Z via an Edit menu. Copy shows a top-right, workspace-tinted **toast** with a filled-accent **Share** button (macOS share sheet), haptic + press feedback, hover-to-keep.

**CURRENT STATE — v0.1.0 (2026-07-21).** Muninn is now a usable **multi-tab browser**: an Arc-style collapsible left sidebar (nav cluster → address bar → vertical tabs → New Tab), a Muninn landing page, Cmd-T/W/L/R (change `e9-multi-tab`). Under it, the full Pass shim is built + green (**42 XCTests**): cross-context bus, content injection + frame registry, cross-context **ports** (`CrossContextPortTests`), and a **native `URLSession` fetch proxy** giving the extension CORS-bypassed `*.proton.me` access (`native-fetch-proxy`, proven at HTTP 200). **The auth-fork login is PARKED at a precise point** (`research/e6-external-gate-2026-07-17.md`): detection clears live, the popup boots, but the fork **consume** throws "Invalid fork state" — the `state`/`localState` we store never matches the fork message's `payload.state`. Everything downstream (consume → `pullFork` via the proxy) is built and waiting. Resume plan in the E6/E7 checkpoints + `native-fetch-proxy`/`e7-minimal-popup` tasks. The paragraphs below are the earlier blow-by-blow (some now superseded — e.g. "missing permissions" was later cleared).

**E5 (general injection — FR-9) is BUILT + green (31 tests; change `e5-injection-frame-registry`).** Unified isolated-world Proxy polyfill (`content-polyfill.js`) with a content-world transport; `InjectionCoordinator` (the FR-9 injector, renamed from the S2-spike `ForkBridgeInjector`) injects the full manifest set — bootstrap → content-polyfill (isolated, `document_start`) → `orchestrator.js` (isolated, `document_end`, all frames) → `webauthn.js` (MAIN, `document_start`); `fork.js` nav-gated to account.proton.me. S2 MAIN-world isolation holds under the full surface. `FrameRegistry` (main id 0, stable subframe ids) backs `webNavigation.getAllFrames`/`getFrame`; `runtime.getFrameId` resolves the caller's frame via `message.frameInfo` (Safari current-frame form; element-arg autofill is post-MVP). Ports **deferred** (Decision 4b — orchestrator initiates none). Orchestrator boot audit: `research/orchestrator-audit-2026-07-17.md` (headless boot is offscreen-limited; the live gate is authoritative).

**E6 blocker RE-DIAGNOSED by the 2026-07-17 live gate** (`research/e5-orchestrator-gate-2026-07-17.md`): with orchestrator.js now injected, the cross-context bus works LIVE (9 relay/response round-trips from the real onboarding page), but **"missing permissions" persists**. Root cause is NOT orchestrator — it's that the account app's presence-check runs in the page **MAIN world via `externally_connectable`** (`chrome.runtime.sendMessage(extId)`, manifest matches account.proton.me + pass.proton.me), which S2 keeps empty. **Next: a narrow MAIN-world `externally_connectable` bridge** (expose only `chrome.runtime = {id, sendMessage, connect}` on those two origins, routed through the bus to `background.js` `onMessageExternal` — the inbound path deferred since E3), then re-attempt the E6 gate. Also pending: E4 (scheme handler / S6). Honor the ratified deviations (FR-12 stub is skeleton scope; FR-13 flows via the vendored fork.js). Not D4 — engine/boot/injection/bus/frame-registry all work. Don't gold-plate — solo passion project.

## Ground rules (non-negotiable)

1. **Never handle Proton credentials.** No asking for, reading, storing, or typing passwords, TOTP codes, or vault contents. All login/unlock actions are performed by Calvin at explicit human gates. Never capture logs/screenshots that would contain credentials or vault data.
2. **Warn Calvin in chat *before* launching anything that opens a GUI window** (apps, test harnesses, WKWebView shells). He works on this Mac and closes unannounced windows — that invalidated three test runs in Spike A. For interactive test gates, launch only when he confirms he's ready.
3. **Human gates at phase transitions.** Calvin approves the PRD, the architecture, and each milestone. Record his verdicts verbatim with timestamps in the relevant artifact.
4. **Execute, don't opine.** Reviews and checks must run the code/tests/flows and cite output. Where a check can be deterministic (DevTools endpoint, log parsing, script), do that before asking a human to eyeball it.
5. Ask before Homebrew installs or any purchase/registration (domains are pending human actions — see `decisions.md`).

## First implementation milestone (after PRD + architecture)

The **walking skeleton**: one window, one WKWebView tab, shim runtime up — login at account.proton.me → vault unlock → autofill on one real site. This retires Spike B's three ordered risks:

1. Auth-fork login flow (`onMessageExternal` bridge injected on `*.proton.me`) — test end-to-end **first**; if login can't complete, nothing else matters.
2. Dropdown-iframe mechanics (custom-scheme iframe in arbitrary pages, CSP interactions).
3. Service-worker global-scope assumptions in `background.js`.

## Key technical facts (from the spikes — trust these, they were measured)

- The shim targets **Proton's Safari build profile**: `storage`, `alarms`, `runtime` messaging + `getURL`, `tabs`, `action`, `windows`, `permissions` (stub), `scripting`, `webNavigation`, `clipboardWrite`. **Skip** `webRequest`, `offscreen`, `privacy`, `commands` — Proton's own Safari build excludes them.
- Four Tier-2 subsystems are the real work: background service-worker host (hidden WKWebView/JSContext — keep alive forever, no MV3 suspend semantics), `WKURLSchemeHandler` for extension resources + web-accessible-resource semantics, `WKContentWorld`/`WKUserScript` injection + frame registry (from `WKNavigationDelegate`/`WKFrameInfo`), and the message broker (`WKScriptMessageHandler` + `evaluateJavaScript`).
- Re-run Spike B's grep inventory against `ProtonMail/WebClients` before writing shim code — it was taken 2026-07-11 vs Pass v1.38.2 and Proton ships often. Their Safari build is the permanent canary for shim parity.
- If the extension is ever side-loaded in a Chromium context (fallback line 3): the CRX public key must be pinned as `"key"` in `manifest.json` or the site→extension login handshake silently breaks (path-derived vs canonical extension ID).
- WKWebView niceties confirmed relevant: Apple Pay JS **disables script injection on active pages** (shim must tolerate suspension on checkout pages); `WKContentRuleList` for blocking; `LocalAuthentication` with `...OrWatch` for vault unlock; `ASAuthorization` for passkeys.

## Naming

Product: **Muninn** (App Store listing: "Muninn Browser"). See `decisions.md` for the availability sweep and pending human actions (defensive domains, trademark counsel).
